"use client";

// Sight has a real mobile layout. The fallback is kept only for extremely
// small viewports (< 360px) where the globe can't render usefully.
export function MobileFallback() {
  return null;
}
